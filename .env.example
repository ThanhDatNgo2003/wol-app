# Wake-on-LAN PWA Configuration

# Command to execute when wake is triggered (required)
# Default: wakepc (assumes you have a 'wakepc' alias/script)
# Example: wakepc, wakeonlan, /usr/local/bin/wakepc
WAKE_CMD=wakepc

# Your PC's IP address (optional, for status check)
# Example: 192.168.1.100
PC_IP=192.168.1.100

# Server port (optional)
# Default: 3000
PORT=3000

# Node environment (optional)
# Default: development
NODE_ENV=production
