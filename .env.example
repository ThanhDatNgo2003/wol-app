# ============================================
# Wake-on-LAN PWA Configuration v2.0
# ============================================

# SECURITY: Authentication
# ============================================
# PIN authentication hash (REQUIRED for security)
# Generate with: node scripts/generate-pin-hash.js
# Use a 4-6 digit numeric PIN (e.g., 1234 or 123456)
PIN_HASH=

# Session secret for secure session management (REQUIRED)
# Generate with: openssl rand -base64 32
# Keep this secret! Never commit to git.
SESSION_SECRET=

# CORS: Allowed origins (optional, defaults to https://wol.thanhdatngo.site)
# Comma-separated list for multiple domains
ALLOWED_ORIGINS=https://wol.thanhdatngo.site

# Wake-on-LAN Configuration
# ============================================
# Command to execute when wake is triggered (required)
# Default: wakepc (assumes you have a 'wakepc' alias/script)
# Example: wakepc, wakeonlan
WAKE_CMD=wakepc

# Your PC's IP address (optional, for status check)
# Example: 192.168.1.100
PC_IP=192.168.1.100

# Server Configuration
# ============================================
# Server port (optional)
# Default: 3000
PORT=3000

# Node environment (optional)
# Default: development
# Use 'production' for HTTPS-only cookies and stricter security
NODE_ENV=production
